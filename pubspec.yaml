name: kec_safety_calculator_core
description: Pure Dart library for KEC electrical safety calculations.
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Pure Dart only

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.0
